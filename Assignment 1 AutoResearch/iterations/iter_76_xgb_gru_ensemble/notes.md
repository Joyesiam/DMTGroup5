# Iteration 76: XGB + GRU Ensemble
**Phase D: Model Improvements**

**Hypothesis:** XGB and GRU capture different patterns (tabular vs temporal). Combined they beat either alone.

**Change:** Train both models, compare complementarity. XGB uses engineered features, GRU uses raw sequences.
